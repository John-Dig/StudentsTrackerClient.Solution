@{
  Layout = "_Layout";
}

@model TrackerClient.Models.Coach

<h2>Details for Coach @Model.FirstN @Model.LastN</h2>


<div class="button-area">
  <a href='@Url.Action("Index", "Home")'>
    <button>Home</button>
  </a>
  @if (User.Identity.IsAuthenticated) {
    <a href='@Url.Action("Edit", "Coaches", new { id = @Model.CoachId })'>
      <button class="yellow">Edit</button>
    </a>
    <a href='@Url.Action("Delete", "Coaches", new { id = @Model.CoachId })'>
      <button class="red">Delete</button>
    </a>
  }
</div>